## What does this change do, and why?

## How was this tested?

- [ ] `ruff check .` passes
- [ ] `pytest tests/ -v` passes
- [ ] Added/updated tests for the change (or explained why none apply)
- [ ] If this adds a new action: manually exercised it against a real
      device/emulator at least once (paste the JSON in/out below)

```json
(optional: real device output showing the new/changed behavior)
```

## Anything reviewers should pay special attention to?
